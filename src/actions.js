export const setItem = (item) => ({
    type: 'SET_ITEM',
    payload: item,
  });
  
export const setFormulation = (formulation) => ({
    type: 'SET_FORMULATION',
    payload: formulation,
  });
  
  export const setBase = (base) => ({
    type: 'SET_BASE',
    payload: base,
  });
  
  export const setConcern = (concern) => ({
    type: 'SET_CONCERN',
    payload: concern,
  });
  
  export const setConcentration = (concentration) => ({
    type: 'SET_CONCENTRATION',
    payload: concentration,
  });
  
  export const setVolume = (volume) => ({
    type: 'SET_VOLUME',
    payload: volume,
  });
  
  export const setCase = (bottle) => ({
    type: 'SET_CASE',
    payload: bottle,
  });

  export const setRemoveIng = (ingredient) => ({
    type: 'SET_INGREDIENT',
    payload: ingredient,
  });

  export const setRecName = (name) => ({
    type: 'SET_NAME',
    payload: name,
  });
  